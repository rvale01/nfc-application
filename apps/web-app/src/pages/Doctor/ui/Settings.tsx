import React, { useEffect } from "react";
import { DoctorPersonalDetailViews } from "ui-web";

export const Settings = () => {
    useEffect(()=> {
        // onLoad get the userid from the local storage
    }, [])
}